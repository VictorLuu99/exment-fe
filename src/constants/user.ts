export const CREATE_ACCOUNT_SIGNED_MESSAGE = 'Register Account';

export enum Roles {
    admin = 'admin',
    buyer = 'buyer',
    seller = 'seller',
}

export enum UserStatusEnum {
    unverified = 'unverified',
    verified = 'verified',
    active = 'active',
}
